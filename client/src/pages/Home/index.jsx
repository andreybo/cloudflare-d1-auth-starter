import { setTitle } from '../../utils/generalFunctions';

export const Home = () => {
    setTitle("Home");

    return (
        <div className="p-8">
            Hello world...
        </div>
    );
}
